@[TOC](股票问题)

---
# [买卖股票的最佳时机](https://leetcode-cn.com/problems/best-time-to-buy-and-sell-stock/)
## 描述  
*简单*  
给定一个数组，它的第 i 个元素是一支给定股票第 i 天的价格。

如果你最多只允许完成一笔交易（即买入和卖出一支股票一次），设计一个算法来计算你所能获取的最大利润。

注意：你不能在买入股票前卖出股票。

 
**示例**
示例 1：

	输入: [7,1,5,3,6,4]
	输出: 5
	解释: 在第 2 天（股票价格 = 1）的时候买入，在第 5 天（股票价格 = 6）的时候卖出，最大利润 = 6-1 = 5 。
	     注意利润不能是 7-1 = 6, 因为卖出价格需要大于买入价格；同时，你不能在买入前卖出股票。
示例 2:

	输入: [7,6,4,3,1]
	输出: 0
	解释: 在这种情况下, 没有交易完成, 所以最大利润为 0。


## 解题  
假设在历史最低点买股票min_price，然后在第i天将股票卖掉，得到的利润为prices[i]-min_price  

然后就是遍历每一天的价格，找到最低的价格和最大的利润  

```python
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        if len(prices) <= 1:
            return 0
        min_value = prices[0]
        max_profit = 0
        for i in range(1, len(prices)):
            max_profit = max(max_profit, prices[i]-min_value)
            min_value = min(min_value, prices[i])
        
        return max_profit
```

# [买卖股票的最佳时机II](https://leetcode-cn.com/problems/best-time-to-buy-and-sell-stock-ii/)
## 描述  
*简单*  
给定一个数组，它的第 i 个元素是一支给定股票第 i 天的价格。

设计一个算法来计算你所能获取的最大利润。你可以尽可能地完成更多的交易（多次买卖一支股票）。

注意：你不能同时参与多笔交易（你必须在再次购买前出售掉之前的股票）。

 
**示例**  
示例 1:

	输入: [7,1,5,3,6,4]
	输出: 7
	解释: 在第 2 天（股票价格 = 1）的时候买入，在第 3 天（股票价格 = 5）的时候卖出, 这笔交易所能获得利润 = 5-1 = 4 。
	     随后，在第 4 天（股票价格 = 3）的时候买入，在第 5 天（股票价格 = 6）的时候卖出, 这笔交易所能获得利润 = 6-3 = 3 。
示例 2:

	输入: [1,2,3,4,5]
	输出: 4
	解释: 在第 1 天（股票价格 = 1）的时候买入，在第 5 天 （股票价格 = 5）的时候卖出, 这笔交易所能获得利润 = 5-1 = 4 。
	     注意你不能在第 1 天和第 2 天接连购买股票，之后再将它们卖出。
	     因为这样属于同时参与了多笔交易，你必须在再次购买前出售掉之前的股票。
示例 3:

	输入: [7,6,4,3,1]
	输出: 0
	解释: 在这种情况下, 没有交易完成, 所以最大利润为 0。
 

提示：

	1 <= prices.length <= 3 * 10 ^ 4
	0 <= prices[i] <= 10 ^ 4

## 解题  
这题不限制买卖股票次数  
第i天比第i-1天价格高，那么就第i-1天买，第i天卖    

可以在假设在一天内把之前的卖了再买

```python
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        profit = 0
        for i in range(1, len(prices)):
            if prices[i] > prices[i-1]:
                profit += prices[i] - prices[i-1]
        return profit
```

# [买卖股票的最佳时机III](https://leetcode-cn.com/problems/best-time-to-buy-and-sell-stock-iii/)  
## 描述  
*困难*   
给定一个数组，它的第 i 个元素是一支给定的股票在第 i 天的价格。

设计一个算法来计算你所能获取的最大利润。你最多可以完成 两笔 交易。

注意: 你不能同时参与多笔交易（你必须在再次购买前出售掉之前的股票）。

**示例**   
示例 1:

	输入: [3,3,5,0,0,3,1,4]
	输出: 6
	解释: 在第 4 天（股票价格 = 0）的时候买入，在第 6 天（股票价格 = 3）的时候卖出，这笔交易所能获得利润 = 3-0 = 3 。
	     随后，在第 7 天（股票价格 = 1）的时候买入，在第 8 天 （股票价格 = 4）的时候卖出，这笔交易所能获得利润 = 4-1 = 3 。
示例 2:

	输入: [1,2,3,4,5]
	输出: 4
	解释: 在第 1 天（股票价格 = 1）的时候买入，在第 5 天 （股票价格 = 5）的时候卖出, 这笔交易所能获得利润 = 5-1 = 4 。   
	     注意你不能在第 1 天和第 2 天接连购买股票，之后再将它们卖出。   
	     因为这样属于同时参与了多笔交易，你必须在再次购买前出售掉之前的股票。
示例 3:

	输入: [7,6,4,3,1] 
	输出: 0 
	解释: 在这个情况下, 没有交易完成, 所以最大利润为 0。

## 解题  
这道题限制了买卖次数，最多进行两次  
那么可以看作是第一题改变了下，把价格分为前后段，然后在每一段中找到利润最大的点，再相加  

然后按照这个思路将价格分段计算，然后就超时了  

```python
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        profit = 0
        for i in range(len(prices)):
            profit1 = self.max_profit(prices[:i])
            profit2 = self.max_profit(prices[i:])
            profit = max(profit, profit1+profit2)

        return profit

    def max_profit(self, prices):
        if len(prices) <= 1:
            return 0
        min_value = prices[0]
        max_profit = 0
        for i in range(1, len(prices)):
            max_profit = max(max_profit, prices[i]-min_value)
            min_value = min(min_value, prices[i])
        
        return max_profit
        

```

上面的方法是每次将价格分为不同的两段，然后计算，效率低下  

按照第一题的思路，从前往后计算一次，从后往前计算一次  

    # 从前往后计算
    min_price = prices[0]
    max_profit = 0
    for i in range(1, len(prices)):
        max_profit = max(max_profit, prices[i]-min_value)
        min_price = min(min_price, prices[i])

    # 从后往前计算
    max_price = prices[-1]
    max_profit = 0
    for i in range(len(prices)-2, -1, -1):
        max_profit = max(max_profit, max_price-prices[i])
        max_price = max(max_price, prices[i])
    

将两个profit变化保存下来

例如，价格为

|价格|3|3|5|0|0|3|1|4|  
|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
|从前往后计算 profit1|0|0|2|2|2|2|2|2|
|从后往前计算 profit2|4|4|4|4|4|3|3|0|

最后通过比较得到profit1+profit2最大的那一列  

比如说，当价格等与5时，利润等于2+4=6最大  
可以看作价格3买进，价格5卖出得到利润2  
然后价格0买进，价格4卖出得到利润4，一共利润6    

也就相当于将把整个价格分成两部分进行买卖  

```python
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        if not prices or len(prices) == 0:
            return 0

        length = len(prices)
        
        min_price = prices[0]   # 前半部分的最小买入价格
        max_price = prices[-1]  # 后半部分的最大卖出价格

        max_profit1 = 0 # 从前往后的利润
        max_profit2 = 0 # 从后往前的利润
        
        profit1 = [0 for i in range(length)] # 从前往后的利润变化
        profit2 = [0 for i in range(length)] # 从后往前的利润变化

        for i in range(length):
            # 从前往后计算
            max_profit1 = max(max_profit1, prices[i] - min_price)
            min_price = min(min_price, prices[i])
            profit1[i] = max_profit1
            # 从后往前计算
            max_profit2 = max(max_profit2, max_price - prices[length-1-i])
            max_price = max(max_price, prices[length-1-i])
            profit2[length-1-i] = max_profit2

        res = 0
        for i in range(length):
            res = max(res, profit1[i]+profit2[i])
        
        return res
```

# [买卖股票的最佳时机IV](https://leetcode-cn.com/problems/best-time-to-buy-and-sell-stock-iv/)

## 描述  
*困难*   
给定一个数组，它的第 i 个元素是一支给定的股票在第 i 天的价格。

设计一个算法来计算你所能获取的最大利润。你最多可以完成 k 笔交易。

注意: 你不能同时参与多笔交易（你必须在再次购买前出售掉之前的股票）。

**示例**   
示例 1:

    输入: [2,4,1], k = 2
    输出: 2
    解释: 在第 1 天 (股票价格 = 2) 的时候买入，在第 2 天 (股票价格 = 4) 的时候卖出，这笔交易所能获得利润 = 4-2 = 2 。
示例 2:

    输入: [3,2,6,5,0,3], k = 2
    输出: 7
    解释: 在第 2 天 (股票价格 = 2) 的时候买入，在第 3 天 (股票价格 = 6) 的时候卖出, 这笔交易所能获得利润 = 6-2 = 4 。
         随后，在第 5 天 (股票价格 = 0) 的时候买入，在第 6 天 (股票价格 = 3) 的时候卖出, 这笔交易所能获得利润 = 3-0 = 3 。

## 解题  
和上一题类似，把最多交易2次买卖变成了更加泛化的k次

看到[大佬](https://leetcode-cn.com/problems/best-time-to-buy-and-sell-stock-iii/solution/yi-ge-tong-yong-fang-fa-tuan-mie-6-dao-gu-piao-wen/)的解法真的是妙啊  


每天有三种的操作，买入、卖出和无操作，





# 参考  
- https://leetcode-cn.com/problems/best-time-to-buy-and-sell-stock-iii/solution/mai-gu-piao-de-zui-jia-shi-ji-iii-yi-kan-jiu-dong-/
- https://leetcode-cn.com/problems/best-time-to-buy-and-sell-stock-iii/solution/yi-ge-tong-yong-fang-fa-tuan-mie-6-dao-gu-piao-wen/